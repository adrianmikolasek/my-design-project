# Eszközök

Helyezd ide a kamion fotót `truck.png` (vagy `.jpg`) néven, és a weboldal automatikusan azt használja a SVG-rajz helyett.

A HTML keresi: `assets/truck.png`

Ha a fájl nincs ott, a beépített SVG ábrázolás látszik fallback-ként.
